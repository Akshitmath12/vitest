// Test generated by RoostGPT for test ReactStoryBook using AI Type Open AI and AI Model gpt-4

const { respondWithMock } = require('./mockServiceWorker');

jest.useFakeTimers();

describe('respondWithMock method', () => {
  test('should correctly delay and return response', async () => {
    const mockResponse = { delay: 1000, body: 'test body' };

    const result = await respondWithMock(mockResponse);

    expect(setTimeout).toHaveBeenCalledTimes(1);
    expect(setTimeout).toHaveBeenLastCalledWith(expect.any(Function), mockResponse.delay);
    expect(result).toBeInstanceOf(Response);
    expect(result.body).toEqual(mockResponse.body);
  });

  test('should handle error when delay is not a number', async () => {
    const mockResponse = { delay: 'invalid', body: 'test body' };

    await expect(respondWithMock(mockResponse)).rejects.toThrow();
  });

  test('should handle error when delay is negative', async () => {
    const mockResponse = { delay: -1000, body: 'test body' };

    await expect(respondWithMock(mockResponse)).rejects.toThrow();
  });
});
