// Test generated by RoostGPT for test ReactStoryBook using AI Type Open AI and AI Model gpt-4

require('@testing-library/jest-dom');
const { setGlobalConfig } = require('@storybook/testing-react');
const { getWorker } = require('msw-storybook-addon');
const globalStorybookConfig = require('../.storybook/preview');

// Set global configuration for @storybook/testing-react
setGlobalConfig(globalStorybookConfig);

describe('Testing getWorker close method', () => {
  let worker;

  beforeAll(() => {
    // Get the worker instance before running the tests
    worker = getWorker();
  });

  test('worker exists', () => {
    // Test to check if the worker exists
    expect(worker).toBeDefined();
  });

  test('worker close method', () => {
    // Mock the close method
    const mockClose = jest.fn();
    worker.close = mockClose;

    // Call the method to be tested
    afterAll(() => worker.close());

    // Test if the close method was called
    expect(mockClose).toHaveBeenCalled();
  });

  test('worker close method not called before afterAll', () => {
    // Mock the close method
    const mockClose = jest.fn();
    worker.close = mockClose;

    // Test if the close method was not called before afterAll
    expect(mockClose).not.toHaveBeenCalled();
  });
});
